/{ 0
			for i := 0; i < 16; i+? {
				X[i&15] = uint32(p[j])
		{{if .Full}}
			// Round 1.
			{{range $i, $s := dup 4 .Shi\t{end}}
			}
	
			// Round 3.
			for i := uint(0); i < 16; {
				{{range $s := .Shift3}}
					{{printf "a += (b^c^